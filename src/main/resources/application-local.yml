# ========================================
# Local 개발 환경 설정
# ========================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/article?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true # jdbc:mysql://ballast.proxy.rlwy.net:38929/railway?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: 0194 #uUqSYuqPJOYxlDRYwigsmbNUYFHotzej
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: anes53027@gmail.com
    password: kjnm ejls xbuu nurb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Gemini API 설정
gemini:
  api-key: AIzaSyAeE4GgzWOoAJuj3DWAcMefMuH6gRodMPQ

# JWT 설정
jwt:
  secret: local-dev-secret-key-must-be-at-least-32-characters-long
  access-token-expire-ms: 1800000      # 30분
  refresh-token-expire-ms: 604800000   # 7일

# 파이프라인 설정
news:
  pipeline:
    enabled: true
    cron: "0 */2 * * * *"  # 2분마다

# CORS 설정
app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
    allowed-methods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
    allowed-headers:
      - "*"
    allow-credentials: true

# 이메일 암호화 키 (32자 이상)
email:
  encryption:
    key: ${EMAIL_ENCRYPTION_KEY:local-email-encryption-key-32chars!}
  verification:
    expire-minutes: 10
    code-length: 6

# 로깅 (Local은 DEBUG 레벨 가능)
logging:
  level:
    com.yourapp.news: DEBUG
